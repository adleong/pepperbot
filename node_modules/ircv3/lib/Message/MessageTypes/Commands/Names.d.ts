import type { MessageParam } from '../../Message';
import { Message } from '../../Message';
export declare class Names extends Message<Names> {
    static readonly SUPPORTS_CAPTURE = true;
    channel: MessageParam;
}
