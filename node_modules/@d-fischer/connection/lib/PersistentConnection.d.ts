import type { Logger } from '@d-fischer/logger';
import type { Constructor } from '@d-fischer/shared-utils';
import { EventEmitter } from '@d-fischer/typed-event-emitter';
import type { ConnectionOptions } from './AbstractConnection';
import type { Connection, ConnectionInfo } from './Connection';
export interface PersistentConnectionConfig {
    retryLimit?: number;
    logger?: Logger;
}
export declare class PersistentConnection<T extends Connection> extends EventEmitter implements Connection {
    private readonly _type;
    private readonly _connectionInfo;
    private readonly _additionalOptions?;
    private readonly _retryLimit;
    private readonly _logger?;
    private _connecting;
    private _retryTimerGenerator?;
    private _connectionRetryCount;
    private _currentConnection?;
    readonly onReceive: import("@d-fischer/typed-event-emitter/lib").EventBinder<[string]>;
    readonly onConnect: import("@d-fischer/typed-event-emitter/lib").EventBinder<[]>;
    readonly onDisconnect: import("@d-fischer/typed-event-emitter/lib").EventBinder<[boolean, (Error | undefined)?]>;
    readonly onEnd: import("@d-fischer/typed-event-emitter/lib").EventBinder<[boolean, (Error | undefined)?]>;
    constructor(_type: Constructor<T>, _connectionInfo: ConnectionInfo, config?: PersistentConnectionConfig, _additionalOptions?: ConnectionOptions<T> | undefined);
    get isConnected(): boolean;
    get isConnecting(): boolean;
    get host(): string;
    get port(): number;
    get hasSocket(): boolean;
    sendLine(line: string): void;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    assumeExternalDisconnect(): void;
    reconnect(): Promise<void>;
    private static _getReconnectWaitTime;
}
